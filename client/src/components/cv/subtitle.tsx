import { Typography, TypographyProps } from '@mui/material';

const SubTitle = (props: TypographyProps) => (
  <Typography color="text.primary" variant="h5" {...props} />
);

export default SubTitle;
